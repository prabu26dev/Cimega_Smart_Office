// Boilerplate for main component
// Currently, Electron directly loads HTML files from src/pages.
// This is a placeholder for future React migration.
import App from './App.jsx';

// If React was installed, we would use:
// import React from 'react';
// import ReactDOM from 'react-dom/client';
// ReactDOM.createRoot(document.getElementById('root')).render(<App />);

console.log("main.jsx loaded. App is running on Electron using direct HTML loading.");
